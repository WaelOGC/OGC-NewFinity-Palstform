# Base URL for API calls from the frontend
# For dev, we go through the Vite proxy and backend /api/v1 routes.
# In production, Nginx proxies /api/* to the backend, and the backend exposes /api/v1/* behind that.
VITE_API_BASE_URL=/api/v1
